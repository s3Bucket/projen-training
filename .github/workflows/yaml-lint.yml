---
name: Change proposal testing
'on':
  push:
    branches:
      - '*'

jobs:

  test-YAML:
    name: Check YAML streams
    uses: evoila/GitHub-Actions/.github/workflows/reusable-YAML.yaml@main
    with:
      python-version-file: .python-version