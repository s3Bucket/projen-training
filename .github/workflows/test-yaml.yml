---
name: Change proposal testing

# Without quotes, key could be parsed as boolean True instead of string "on"
'on':
  push:
    branches:
      - '*'

jobs:

  test-YAML:
    name: Check YAML streams
    uses: evoila/GitHub-Actions/.github/workflows/reusable-YAML.yaml@v0.6.0
    with:
      python-version-file: .python-version
